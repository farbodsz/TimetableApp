<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.timetableapp"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for notifications -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Required for export/import -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk tools:overrideLibrary="com.jakewharton.threetenabp" />

    <application
        android:name=".TimetableApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Timetable"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:name=".ui.start.WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Timetable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.start.InitialSetupActivity"
            android:label="@string/app_name"
            android:theme="@style/Timetable" />

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Timetable.Home" />

        <activity
            android:name="co.timetableapp.ui.ScheduleActivity"
            android:label="@string/title_activity_schedule"
            android:theme="@style/Timetable.Home" />

        <activity
            android:name="co.timetableapp.ui.ClassesActivity"
            android:label="@string/title_activity_classes"
            android:theme="@style/Timetable.Home" />

        <activity
            android:name=".ui.ClassDetailActivity"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.ClassEditActivity"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.ClassTimeEditActivity"
            android:theme="@style/Timetable.EditItem"/>

        <activity
            android:name="co.timetableapp.ui.AssignmentsActivity"
            android:label="@string/title_activity_assignments"
            android:theme="@style/Timetable.Home"/>

        <activity
            android:name=".ui.AssignmentDetailActivity"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.AssignmentEditActivity"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.ExamsActivity"
            android:label="@string/title_activity_exams"
            android:theme="@style/Timetable.Home" />

        <activity
            android:name=".ui.ExamDetailActivity"
            android:theme="@style/Timetable" />

        <activity
            android:name="co.timetableapp.ui.ExamEditActivity"
            android:theme="@style/Timetable" />

        <activity
            android:name="co.timetableapp.ui.SubjectsActivity"
            android:label="@string/title_activity_subjects"
            android:theme="@style/Timetable.Home"/>

        <activity
            android:name="co.timetableapp.ui.SubjectEditActivity"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.TimetablesActivity"
            android:label="@string/title_activity_timetables"
            android:theme="@style/Timetable"/>

        <activity
            android:name="co.timetableapp.ui.TimetableEditActivity"
            android:theme="@style/Timetable.EditItem"/>

        <activity
            android:name="co.timetableapp.ui.TermEditActivity"
            android:theme="@style/Timetable.EditItem"/>

        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/action_settings"
            android:theme="@style/Timetable"/>

        <activity
            android:name=".licenses.LicensesActivity"
            android:label="@string/title_licenses"
            android:theme="@style/Timetable" />

        <activity
            android:name=".licenses.LibraryDetailActivity"
            android:theme="@style/Timetable" />

        <receiver android:name="co.timetableapp.receiver.AlarmReceiver" />

        <receiver android:name=".receiver.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>
